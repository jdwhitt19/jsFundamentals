// Literals

// JS defines 2 types of values:
// 1 - fixed values (called literals)
// 2 - Variable values (called variables)

// Literals include: arrays, numbers, booleans, objects, and strings

let days = ['Monday', 'Tuesday', 'Wedensday', 'Thursday', 'Friday']
console.log(days.toString());

console.log(days.length);

let tired = true;

let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    c: 'beef and barley'
};
console.log(soup.a);

let car = 'Honda';

let decimal = 10.2;